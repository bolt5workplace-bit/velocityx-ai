<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Dashboard - Trading Platform</title>
  <link rel="stylesheet" href="/css/styles.css">
  <link href="https://i.imgur.com/xnzyBi9.png" rel="shortcut icon"/>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&display=swap" rel="stylesheet">
  <script src="https://unpkg.com/lucide@latest"></script>
  <script src="/js/formatters.js"></script>
  <style>
:root {
  --primary-bg: #0B0F19;
  --secondary-bg: #111827;
  --card-bg: #1F2937;
  --card-elevated: #374151;
  --text-primary: #F9FAFB;
  --text-secondary: #D1D5DB;
  --text-muted: #9CA3AF;
  --accent-emerald: #3477F2;
  --accent-emerald-hover: #2b62c9;
  --accent-teal: #14B8A6;
  --accent-cyan: #06B6D4;
  --accent-blue: #3B82F6;
  --accent-orange: #F59E0B;
  --danger-red: #EF4444;
  --border-color: rgba(255, 255, 255, 0.06);
  --border-hover: rgba(255, 255, 255, 0.12);
  --shadow-sm: 0 1px 2px 0 rgba(0, 0, 0, 0.4);
  --shadow-md: 0 4px 6px -1px rgba(0, 0, 0, 0.5);
  --shadow-lg: 0 10px 15px -3px rgba(0, 0, 0, 0.6);
  --shadow-xl: 0 20px 25px -5px rgba(0, 0, 0, 0.7);
  --shadow-2xl: 0 25px 50px -12px rgba(0, 0, 0, 0.8);
}

* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

body {
  font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
  background: var(--primary-bg);
  color: var(--text-primary);
  line-height: 1;
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
  font-feature-settings: "ss01", "cv01", "cv03";
}

.dashboard-container {
  min-height: 100vh;
  display: flex;
  background: linear-gradient(135deg, #0B0F19 0%, #111827 50%, #0B0F19 100%);
  position: relative;
}

.dashboard-container::before {
  content: '';
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  height: 400px;
  background: radial-gradient(circle at 50% 0%, rgba(52, 119, 242, 0.08), transparent 70%);
  pointer-events: none;
  z-index: 0;
}

.dashboard-content {
  flex: 1;
  padding: 1rem;
  overflow-y: auto;
  max-width: 100%;
  position: relative;
  z-index: 1;
}

@media (max-width: 768px) {
  .dashboard-content {
    padding: 0.8;
  }
}

.vx_dashboard_header_section {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 3rem;
  padding-bottom: 2rem;
  border-bottom: 1px solid var(--border-color);
  animation: slideDown 0.6s ease;
}

@keyframes slideDown {
  from {
    opacity: 0;
    transform: translateY(-20px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.vx_header_title h1 {
  font-size: 1.95rem;
  font-weight: 800;
  color: var(--text-primary);
  margin-bottom: 0.5rem;
  letter-spacing: -0.04em;
  background: linear-gradient(135deg, #F9FAFB 0%, #D1D5DB 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}

.vx_header_title p {
  color: var(--text-secondary);
  font-size: 0.95rem;
  font-weight: 400;
  letter-spacing: -0.01em;
}

.vx_btn_invest_now {
  display: inline-flex;
  align-items: center;
  gap: 0.625rem;
  padding: 1rem 2rem;
  background: linear-gradient(135deg, var(--accent-emerald) 0%, var(--accent-teal) 100%);
  color: white;
  border: none;
  border-radius: 12px;
  font-weight: 700;
  font-size: 0.95rem;
  text-decoration: none;
  cursor: pointer;
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  box-shadow: 0 8px 24px rgba(16, 185, 129, 0.3), 0 0 0 0 rgba(16, 185, 129, 0.4);
  position: relative;
  overflow: hidden;
}

.vx_btn_invest_now::before {
  content: '';
  position: absolute;
  top: 0;
  left: -100%;
  width: 100%;
  height: 100%;
  background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.3), transparent);
  transition: left 0.5s ease;
}

.vx_btn_invest_now:hover::before {
  left: 100%;
}

.vx_btn_invest_now:hover {
  transform: translateY(-2px) scale(1.02);
  box-shadow: 0 12px 32px rgba(16, 185, 129, 0.4), 0 0 0 4px rgba(16, 185, 129, 0.1);
}

.vx_btn_invest_now:active {
  transform: translateY(0) scale(0.98);
}

.vx_btn_invest_now svg {
  width: 20px;
  height: 20px;
  transition: transform 0.3s ease;
}

.vx_btn_invest_now:hover svg {
  transform: translateX(3px);
}

@media (max-width: 738px) {
  .vx_dashboard_header_section {
    flex-direction: column;
    gap: 1rem;
    text-align: center;
  }

  .vx_header_title h1 {
    font-size: 1rem !important;
  }

  .vx_header_title p {
    font-size: 0.7rem !important;
    font-family:'Times New Roman', Times, serif !important;
  }

  .vx_btn_invest_now {
    width: fit-content;
    justify-content: center;
    font-size: 0.9rem !important;
  }
}

.vx_account_balance_card {
  background: linear-gradient(135deg, #1F2937 0%, #111827 100%);
  border: 1px solid var(--border-color);
  border-radius: 24px;
  padding: 1.5rem 1rem;
  margin-bottom: 2rem;
  text-align: center;
  position: relative;
  overflow: hidden;
  box-shadow: var(--shadow-2xl);

  animation: none;
}

.vx_account_balance_card::after {
  content: '';
  position: absolute;
  top: -50%;
  left: -50%;
  width: 200%;
  height: 200%;
  background: radial-gradient(circle, rgba(52, 119, 242, 0.08) 0%, transparent 70%);
  pointer-events: none;
}

.vx_balance_icon {
  font-size: 2rem;
  margin-bottom: 1.25rem;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  width: 80px;
  height: 80px;
  background: linear-gradient(135deg, rgba(52, 119, 242, 0.15), rgba(37, 99, 235, 0.15));
  border-radius: 50%;
  position: relative;
  z-index: 1;
  box-shadow: 0 8px 32px rgba(52, 119, 242, 0.2);
}

.vx_balance_icon svg {
  width: 40px;
  height: 40px;
  color: #3477F2;
}

.vx_balance_title {
  font-size: 0.8rem;
  color: var(--text-muted);
  text-transform: uppercase;
  letter-spacing: 2px;
  margin-bottom: 0.5rem;
  font-weight: 700;
  position: relative;
  z-index: 1;
}

.vx_balance_subtitle {
  font-size: 0.875rem;
  color: var(--text-secondary);
  margin-bottom: 1.5rem;
  font-weight: 500;
  position: relative;
  z-index: 1;
}

.vx_balance_amount {
  font-size: 4rem;
  font-weight: 900;
  color: #3477F2;
  margin-bottom: 1.5rem;
  letter-spacing: -0.04em;
  position: relative;
  z-index: 1;
}

.vx_balance_status {
  display: inline-flex;
  align-items: center;
  gap: 0.5rem;
  font-size: 0.875rem;
  margin: 0.5rem 0.75rem;
  padding: 0.625rem 1.25rem;
  border-radius: 10px;
  font-weight: 700;
  position: relative;
  z-index: 1;
  transition: all 0.3s ease;
}

.vx_balance_status:hover {
  transform: translateY(-2px);
}

.vx_balance_status svg {
  width: 18px;
  height: 18px;
}

.vx_balance_status.success {
  color: #3477F2;
  background: rgba(52, 119, 242, 0.15);
  border: 1px solid rgba(52, 119, 242, 0.3);
  box-shadow: 0 4px 16px rgba(52, 119, 242, 0.2);
}

.vx_balance_status.unverified {
  color: var(--danger-red);
  background: rgba(239, 68, 68, 0.15);
  border: 1px solid rgba(239, 68, 68, 0.3);
  box-shadow: 0 4px 16px rgba(239, 68, 68, 0.2);
}

.vx_balance_updated {
  font-size: 0.8rem;
  color: var(--text-muted);
  margin-bottom: 2rem;
  display: inline-flex;
  align-items: center;
  gap: 0.5rem;
  position: relative;
  z-index: 1;
  font-weight: 500;
}

.vx_balance_updated svg {
  width: 16px;
  height: 16px;
}

.vx_balance_actions {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 1rem;
  flex-wrap: wrap;
  margin-top: 1.5rem;
}

.vx_btn_deposit,
.vx_btn_withdraw {
  min-width: 140px;
  padding: 0.95rem 1.3rem;
}

.vx_btn_deposit svg,
.vx_btn_withdraw svg {
  width: 20px;
  height: 20px;
  transition: transform 0.3s ease;
}

.vx_btn_deposit {
  background: linear-gradient(135deg, #3477F2 0%, #2563eb 100%);
  color: white;
  box-shadow: 0 8px 24px rgba(52, 119, 242, 0.3);
}

.vx_btn_deposit::before {
  content: '';
  position: absolute;
  top: 0;
  left: -100%;
  width: 100%;
  height: 100%;
  background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.3), transparent);
  transition: left 0.5s ease;
}

.vx_btn_deposit:hover::before {
  left: 100%;
}

.vx_btn_deposit:hover {
  transform: translateY(-3px);
  box-shadow: 0 12px 32px rgba(52, 119, 242, 0.4);
}

.vx_btn_deposit:hover svg {
  transform: translateY(-2px);
}

.vx_btn_withdraw {
  background: var(--card-bg);
  color: var(--accent-cyan);
  border: 2px solid rgba(6, 182, 212, 0.3);
  box-shadow: 0 4px 16px rgba(6, 182, 212, 0.2);
}

.vx_btn_withdraw:hover {
  background: rgba(6, 182, 212, 0.1);
  border-color: rgba(6, 182, 212, 0.5);
  transform: translateY(-3px);
  box-shadow: 0 12px 32px rgba(6, 182, 212, 0.3);
}

.vx_btn_withdraw:hover svg {
  transform: translateY(2px);
}

@media (max-width: 768px) {
  .vx_account_balance_card {
    padding: 1rem 0.7rem;
  }

  .vx_balance_amount {
    font-size: 1.95rem;
  }

  .vx_balance_actions {
    gap: 1rem;
  }
}

.vx_stats_row {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 1rem;
  margin-bottom: 3rem;
}

.vx_active_engagements {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 1rem;
  margin-bottom: 2rem;
}

.vx_active_trade_card {
  background: linear-gradient(135deg, var(--card-bg) 0%, var(--secondary-bg) 100%);
  border: 1px solid rgba(148, 163, 184, 0.18);
  border-radius: 18px;
  padding: 1.5rem;
}

.vx_active_trade_card h4 {
  margin: 0 0 0.75rem;
  font-size: 1rem;
  color: #E2E8F0;
}

.vx_active_trade_card p {
  margin: 0.35rem 0;
  color: #94A3B8;
}

.vx_active_trade_list {
  display: grid;
  gap: 1rem;
  margin-bottom: 2rem;
}

.vx_active_trade_list_row {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 1rem;
  background: rgba(255, 255, 255, 0.04);
  border: 1px solid rgba(148, 163, 184, 0.08);
  border-radius: 14px;
  padding: 1rem 1.25rem;
}

.vx_active_trade_list_item {
  display: flex;
  flex-direction: column;
  gap: 0.25rem;
}

.vx_active_trade_tag {
  font-size: 0.8rem;
  color: #60A5FA;
  font-weight: 700;
}

.vx_active_trade_status {
  font-size: 0.85rem;
  color: #22C55E;
  font-weight: 700;
}

.vx_section_verification {
  background: linear-gradient(135deg, var(--card-bg) 0%, var(--secondary-bg) 100%);
  border: 1px solid var(--border-color);
  border-radius: 20px;
  padding: 1rem;
  transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
  position: relative;
  overflow: hidden;
  box-shadow: var(--shadow-lg);
  animation: fadeInUp 0.7s ease backwards;
}

.vx_stat_card:nth-child(1) { animation-delay: 0.1s; }
.vx_stat_card:nth-child(2) { animation-delay: 0.2s; }
.vx_stat_card:nth-child(3) { animation-delay: 0.3s; }
.vx_stat_card:nth-child(4) { animation-delay: 0.4s; }

@keyframes fadeInUp {
  from {
    opacity: 0;
    transform: translateY(30px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.vx_stat_card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 3px;
  background: linear-gradient(90deg, #3477F2, #2563eb);
  transform: scaleX(0);
  transform-origin: left;
  transition: transform 0.4s ease;
}

.vx_stat_card:hover::before {
  transform: scaleX(1);
}

.vx_stat_card::after {
  content: '';
  position: absolute;
  top: -50%;
  right: -50%;
  width: 100%;
  height: 200%;
  background: radial-gradient(circle, rgba(52, 119, 242, 0.06) 0%, transparent 70%);
  opacity: 0;
  transition: opacity 0.4s ease;
  pointer-events: none;
}

.vx_stat_card:hover::after {
  opacity: 1;
}

.vx_stat_card:hover {
  border-color: var(--border-hover);
  transform: translateY(-6px) scale(1.02);
  box-shadow: 0 16px 48px rgba(0, 0, 0, 0.6);
}

.vx_stat_header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 1.25rem;
}

.vx_stat_icon {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 28px;
  height: 28px;
  border-radius: 14px;
  background: linear-gradient(135deg, rgba(52, 119, 242, 0.15), rgba(37, 99, 235, 0.1));
  transition: all 0.3s ease;
}

.vx_stat_card:hover .vx_stat_icon {
  transform: scale(1.1) rotate(5deg);
  box-shadow: 0 8px 24px rgba(52, 119, 242, 0.3);
}

.vx_stat_icon svg {
  width: 14px;
  height: 14px;
  color: #3477F2;
}

.vx_stat_label {
  font-size: 0.75rem;
  color: var(--text-muted);
  text-transform: uppercase;
  letter-spacing: 1.5px;
  font-weight: 700;
}

.vx_stat_value {
  font-size: 2.25rem;
  font-weight: 900;
  color: var(--text-primary);
  margin-bottom: 0.75rem;
  letter-spacing: -0.03em;
  transition: all 0.3s ease;
}

.vx_stat_card:hover .vx_stat_value {
  color: #3477F2;
}

.vx_stat_subtitle {
  font-size: 0.75rem;
  color: var(--text-secondary);
  display: flex;
  align-items: center;
  gap: 0.375rem;
  font-weight: 500;
}

.vx_stat_subtitle svg {
  width: 14px;
  height: 14px;
}

@media (max-width: 1024px) {
  .vx_stats_row {
    grid-template-columns: repeat(2, 1fr);
    gap: 1.25rem;
  }
}

@media (max-width: 768px) {
  .vx_stat_icon svg {
  width: 10px;
  height: 10px;
  color: var(--accent-emerald);
}

.vx_stat_label {
  font-size: 0.35rem;
  color: var(--text-muted);
  text-transform: uppercase;
  letter-spacing: 1.5px;
  font-weight: 700;
}

.vx_stat_label {
  font-size: 0.75rem;
  color: var(--text-muted);
  text-transform: uppercase;
  letter-spacing: 0.5px;
  font-weight: 700;
}
  .vx_stats_row {
    grid-template-columns: repeat(2, 1fr);
    gap: 1rem;
    margin-bottom: 2rem;
  }

  .vx_stat_card {
    padding: 1rem;
  }

  .vx_stat_value {
    font-size: 1.25rem;
  }
}

.vx_verification_section {
  background: linear-gradient(135deg, rgba(16, 185, 129, 0.12) 0%, rgba(20, 184, 166, 0.08) 100%);
  border: 1px solid rgba(16, 185, 129, 0.25);
  border-radius: 20px;
  padding: 1.75rem 2rem;
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 3rem;
  position: relative;
  overflow: hidden;
  box-shadow: 0 8px 32px rgba(16, 185, 129, 0.15);
  animation: fadeInUp 0.8s ease backwards;
  animation-delay: 0.5s;
}

.vx_verification_section::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  width: 5px;
  height: 100%;
  background: linear-gradient(180deg, var(--accent-emerald), var(--accent-teal));
}

.vx_verification_section::after {
  content: '';
  position: absolute;
  top: -50%;
  left: -50%;
  width: 200%;
  height: 200%;
  background: radial-gradient(circle, rgba(16, 185, 129, 0.08) 0%, transparent 70%);
  animation: rotate 15s linear infinite;
  pointer-events: none;
}

.vx_verification_content {
  display: flex;
  align-items: center;
  gap: 1.5rem;
  padding-left: 1rem;
  position: relative;
  z-index: 1;
}

.vx_verification_icon {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 64px;
  height: 64px;
  background: linear-gradient(135deg, rgba(52, 119, 242, 0.2), rgba(37, 99, 235, 0.15));
  border-radius: 50%;
  box-shadow: 0 8px 24px rgba(52, 119, 242, 0.3);
}

.vx_verification_icon svg {
  width: 32px;
  height: 32px;
  color: #3477F2;
}

.vx_verification_text h3 {
  font-size: 1.125rem;
  font-weight: 800;
  color: var(--text-primary);
  margin-bottom: 0.375rem;
  letter-spacing: -0.02em;
}

.vx_verification_text p {
  color: var(--text-secondary);
  font-size: 0.875rem;
  font-weight: 500;
}

.vx_btn_view_details {
  padding: 1rem 2rem;
  background: linear-gradient(135deg, #3477F2 0%, #2563eb 100%);
  color: white;
  border: none;
  border-radius: 12px;
  font-weight: 700;
  font-size: 0.95rem;
  text-decoration: none;
  cursor: pointer;
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  display: inline-flex;
  align-items: center;
  gap: 0.625rem;
  box-shadow: 0 8px 24px rgba(52, 119, 242, 0.3);
  position: relative;
  z-index: 1;
  overflow: hidden;
}

.vx_btn_view_details::before {
  content: '';
  position: absolute;
  top: 0;
  left: -100%;
  width: 100%;
  height: 100%;
  background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.3), transparent);
  transition: left 0.5s ease;
}

.vx_btn_view_details:hover::before {
  left: 100%;
}

.vx_btn_view_details svg {
  width: 18px;
  height: 18px;
  transition: transform 0.3s ease;
}

.vx_btn_view_details:hover {
  transform: translateY(-2px) scale(1.02);
  box-shadow: 0 12px 32px rgba(52, 119, 242, 0.4);
}

.vx_btn_view_details:hover svg {
  transform: translateX(3px);
}

@media (max-width: 768px) {
  .vx_verification_section {
    flex-direction: column;
    gap: 1.5rem;
    text-align: center;
    padding: 2rem 1.5rem;
  }

  .vx_verification_content {
    flex-direction: column;
    padding-left: 0;
  }

  .vx_btn_view_details {
    width: 100%;
    justify-content: center;
  }
}

.vx_layout_two_col {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 2rem;
  margin-bottom: 3rem;
  width: 100% !important;
  width: fit-content !important;
}

@media (max-width: 1024px) {
  .vx_layout_two_col {
    grid-template-columns: 1fr;
    gap: 2rem;
    width: 100%;
  }
}

.vx_section_trades,
.vx_section_referrals {
  background: linear-gradient(135deg, var(--card-bg) 0%, var(--secondary-bg) 100%);
  border: 1px solid var(--border-color);
  border-radius: 20px;
  padding: 16px;

  width: 100%;
  max-width: 100%;
  box-sizing: border-box;

  overflow: hidden;
}
* {
  box-sizing: border-box;
  max-width: 100%;
}
@media (max-width: 480px) {
  .vx_section_trades,
  .vx_section_referrals {
    padding: 12px;
    border-radius: 14px;
  }
}
.vx_section_trades * ,
.vx_section_referrals * {
  max-width: 100%;
  word-break: break-word;
  flex-wrap: wrap;
}



.vx_section_trades {
  animation-delay: 0.6s;
}

.vx_section_referrals {
  animation-delay: 0.7s;
}

.vx_section_trades:hover,
.vx_section_referrals:hover {
  border-color: var(--border-hover);
  transform: translateY(-4px);
  box-shadow: 0 16px 48px rgba(0, 0, 0, 0.6);
}

.vx_section_title {
  font-size: 1.25rem;
  font-weight: 800;
  color: var(--text-primary);
  margin-bottom: 2rem;
  display: flex;
  align-items: center;
  gap: 0.75rem;
  letter-spacing: -0.02em;
}

.vx_section_title svg {
  width: 24px;
  height: 24px;
  color: #3477F2;
}

.vx_trades_table {
  width: 100%;
  border-collapse: separate;
  border-spacing: 0;
  margin-bottom: 1.5rem;
}

.vx_trades_table thead {
  background: rgba(255, 255, 255, 0.02);
}

.vx_trades_table th {
  padding: 1rem;
  text-align: left;
  font-weight: 700;
  color: var(--text-muted);
  font-size: 0.75rem;
  text-transform: uppercase;
  letter-spacing: 1.5px;
  border-bottom: 2px solid var(--border-color);
}

.vx_trades_table td {
  padding: 1.125rem 1rem;
  color: var(--text-primary);
  border-bottom: 1px solid rgba(255, 255, 255, 0.03);
  font-size: 0.9rem;
  font-weight: 500;
}

.vx_link_view_all {
  color: #3477F2;
  text-decoration: none;
  font-weight: 700;
  font-size: 0.95rem;
  transition: all 0.3s ease;
  display: inline-flex;
  align-items: center;
  gap: 0.5rem;
}

.vx_link_view_all svg {
  width: 18px;
  height: 18px;
  transition: transform 0.3s ease;
}

.vx_link_view_all:hover {
  color: #2563eb;
  gap: 0.75rem;
}

.vx_link_view_all:hover svg {
  transform: translateX(3px);
}

.vx_referral_text {
  color: var(--text-secondary);
  font-size: 0.95rem;
  margin-bottom: 1.5rem;
  line-height: 1.7;
  font-weight: 400;
}

.vx_btn_learn_more {
  display: inline-flex;
  align-items: center;
  gap: 0.625rem;
  padding: 0.875rem 1.5rem;
  background: linear-gradient(135deg, #3477F2 0%, #2563eb 100%);
  color: white;
  border: none;
  border-radius: 12px;
  font-weight: 700;
  font-size: 0.9rem;
  text-decoration: none;
  cursor: pointer;
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  margin-bottom: 2rem;
  box-shadow: 0 6px 20px rgba(52, 119, 242, 0.3);
}

.vx_btn_learn_more svg {
  width: 18px;
  height: 18px;
  transition: transform 0.3s ease;
}

.vx_btn_learn_more:hover {
  transform: translateY(-2px);
  box-shadow: 0 10px 28px rgba(52, 119, 242, 0.4);
}

.vx_btn_learn_more:hover svg {
  transform: scale(1.2) rotate(15deg);
}

.vx_referral_link_box {
  background: linear-gradient(135deg, rgba(52, 119, 242, 0.08), rgba(37, 99, 235, 0.05));
  border: 1px solid rgba(52, 119, 242, 0.2);
  border-radius: 16px;
  padding: 1.5rem;
  box-shadow: 0 4px 16px rgba(52, 119, 242, 0.1);
}

.vx_referral_label {
  font-size: 0.8rem;
  font-weight: 700;
  color: #3477F2;
  margin-bottom: 0.875rem;
  display: flex;
  align-items: center;
  gap: 0.5rem;
  text-transform: uppercase;
  letter-spacing: 1px;
}

.vx_referral_label svg {
  width: 16px;
  height: 16px;
}

.vx_referral_link_input {
  display: flex;
  gap: 0.75rem;
}

.vx_referral_link_input input {
  flex: 1;
  background: var(--secondary-bg);
  border: 1px solid var(--border-color);
  border-radius: 10px;
  padding: 0.875rem 1rem;
  color: var(--text-primary);
  font-size: 0.85rem;
  font-family: 'Inter', monospace;
  transition: all 0.3s ease;
  font-weight: 500;
}

.vx_referral_link_input input:focus {
  outline: none;
  border-color: var(--accent-emerald);
  box-shadow: 0 0 0 4px rgba(16, 185, 129, 0.15);
  background: var(--primary-bg);
}

.vx_referral_link_input input::placeholder {
  color: var(--text-muted);
}

.vx_btn_copy {
  padding: 0.875rem 1.5rem;
  background: linear-gradient(135deg, var(--accent-emerald) 0%, var(--accent-teal) 100%);
  color: white;
  border: none;
  border-radius: 10px;
  font-weight: 700;
  cursor: pointer;
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  font-size: 0.9rem;
  display: flex;
  align-items: center;
  gap: 0.5rem;
  box-shadow: 0 4px 16px rgba(16, 185, 129, 0.3);
}

.vx_btn_copy svg {
  width: 18px;
  height: 18px;
}

.vx_btn_copy:hover {
  transform: translateY(-2px);
  box-shadow: 0 8px 24px rgba(16, 185, 129, 0.4);
}

.vx_btn_copy:active {
  transform: scale(0.95);
}

.vx_market_tickers_section {
  margin-bottom: 3rem;
  animation: fadeInUp 0.8s ease backwards;
  animation-delay: 0.8s;
}

.vx_market_tickers {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
  gap: 1.5rem;
}

.vx_ticker_card {
  background: linear-gradient(135deg, var(--card-bg) 0%, var(--secondary-bg) 100%);
  border: 1px solid var(--border-color);
  border-radius: 20px;
  padding: 1.75rem;
  transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
  cursor: pointer;
  text-decoration: none;
  color: inherit;
  display: block;
  position: relative;
  overflow: hidden;
  box-shadow: var(--shadow-lg);
}

.vx_ticker_card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 3px;
  background: linear-gradient(90deg, var(--accent-emerald), var(--accent-teal));
  transform: scaleX(0);
  transition: transform 0.4s ease;
}

.vx_ticker_card:hover::before {
  transform: scaleX(1);
}

.vx_ticker_card:hover {
  border-color: var(--border-hover);
  background: linear-gradient(135deg, var(--card-elevated) 0%, var(--card-bg) 100%);
  transform: translateY(-6px) scale(1.02);
  box-shadow: 0 16px 48px rgba(0, 0, 0, 0.6);
}

.vx_ticker_header {
  display: flex;
  align-items: center;
  gap: 1rem;
  margin-bottom: 1.25rem;
}

.vx_ticker_logo {
  width: 44px;
  height: 44px;
  border-radius: 50%;
  object-fit: cover;
  box-shadow: 0 4px 16px rgba(0, 0, 0, 0.4);
  transition: transform 0.3s ease;
}

.vx_ticker_card:hover .vx_ticker_logo {
  transform: scale(1.1) rotate(5deg);
}

.vx_ticker_info {
  display: flex;
  flex-direction: column;
}

.vx_ticker_pair {
  font-weight: 800;
  color: var(--text-primary);
  font-size: 1.05rem;
  letter-spacing: -0.02em;
}

.vx_ticker_exchange {
  font-size: 0.7rem;
  color: var(--text-muted);
  text-transform: uppercase;
  letter-spacing: 1px;
  font-weight: 700;
  margin-top: 2px;
}

.vx_ticker_price {
  font-size: 1.75rem;
  font-weight: 900;
  color: var(--text-primary);
  margin-bottom: 0.75rem;
  letter-spacing: -0.03em;
}

.vx_ticker_change {
  font-weight: 800;
  font-size: 0.875rem;
  padding: 0.5rem 0.875rem;
  border-radius: 10px;
  display: inline-flex;
  align-items: center;
  gap: 0.375rem;
  width: fit-content;
  transition: all 0.3s ease;
}

.vx_ticker_change svg {
  width: 16px;
  height: 16px;
}

.vx_ticker_change.up {
  background: rgba(16, 185, 129, 0.2);
  color: var(--accent-emerald);
  border: 1px solid rgba(16, 185, 129, 0.3);
}

.vx_ticker_change.down {
  background: rgba(239, 68, 68, 0.2);
  color: var(--danger-red);
  border: 1px solid rgba(239, 68, 68, 0.3);
}

.vx_ticker_chart {
  margin-top: 1rem;
  height: 48px;
  position: relative;
  border-radius: 8px;
  overflow: hidden;
}

.market-overview-section {
  background: linear-gradient(135deg, var(--card-bg) 0%, var(--secondary-bg) 100%);
  border: 1px solid var(--border-color);
  border-radius: 20px;
  margin-bottom: 3rem;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
  box-shadow: var(--shadow-lg);
  animation: fadeInUp 0.8s ease backwards;
  animation-delay: 0.9s;
}

.market-overview-section:hover {
  border-color: var(--border-hover);
  transform: translateY(-4px);
  box-shadow: 0 16px 48px rgba(0, 0, 0, 0.6);
}

.market-overview-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 2rem;
  border-bottom: 1px solid var(--border-color);
  background: rgba(255, 255, 255, 0.02);
}

.market-overview-title {
  font-size: 1.25rem;
  font-weight: 800;
  color: var(--text-primary);
  display: flex;
  align-items: center;
  gap: 0.75rem;
  letter-spacing: -0.02em;
}

.market-overview-title svg {
  width: 24px;
  height: 24px;
  color: var(--accent-emerald);
}

.market-overview-link {
  color: var(--accent-emerald);
  text-decoration: none;
  font-size: 0.95rem;
  font-weight: 700;
  transition: all 0.3s ease;
  display: inline-flex;
  align-items: center;
  gap: 0.5rem;
}

.market-overview-link svg {
  width: 18px;
  height: 18px;
  transition: transform 0.3s ease;
}

.market-overview-link:hover {
  color: var(--accent-teal);
  gap: 0.75rem;
}

.market-overview-link:hover svg {
  transform: translate(2px, -2px);
}

.trading-chart-container {
  height: 450px;
  background: linear-gradient(180deg, #0B0F19, #111827);
  overflow: hidden;
}

.chart-widget {
  height: 100%;
  min-height: 430px;
}

@media (max-width: 768px) {
  .trading-chart-container {
    height: 300px;
  }

  .chart-widget {
    min-height: 280px;
  }
}

.vx_table_section {
  background: linear-gradient(135deg, var(--card-bg) 0%, var(--secondary-bg) 100%);
  border: 1px solid var(--border-color);
  border-radius: 20px;
  margin-bottom: 3rem;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
  box-shadow: var(--shadow-lg);
  animation: fadeInUp 0.8s ease backwards;
}

.vx_table_section:nth-of-type(10) { animation-delay: 1s; }
.vx_table_section:nth-of-type(11) { animation-delay: 1.1s; }

.vx_table_section:hover {
  border-color: var(--border-hover);
  transform: translateY(-4px);
  box-shadow: 0 16px 48px rgba(0, 0, 0, 0.6);
}

.vx_table_header {
  background: rgba(255, 255, 255, 0.02);
  border-bottom: 1px solid var(--border-color);
  padding: 1.5rem 2rem;
  display: flex;
  align-items: center;
  gap: 0.75rem;
}

.vx_table_header svg {
  width: 22px;
  height: 22px;
  color: var(--accent-emerald);
}

.vx_table_header h3 {
  font-size: 0.875rem;
  font-weight: 800;
  color: var(--text-primary);
  text-transform: uppercase;
  letter-spacing: 2px;
}

.vx_data_table {
  width: 100%;
  border-collapse: separate;
  border-spacing: 0;
}

.vx_data_table thead {
  background: rgba(255, 255, 255, 0.01);
}

.vx_data_table th {
  padding: 1.125rem 1.5rem;
  text-align: left;
  font-weight: 700;
  color: var(--text-muted);
  font-size: 0.75rem;
  text-transform: uppercase;
  letter-spacing: 1.5px;
  border-bottom: 2px solid var(--border-color);
}

.vx_data_table tbody tr {
  border-bottom: 1px solid rgba(255, 255, 255, 0.03);
  transition: all 0.2s ease;
}

.vx_data_table tbody tr:hover {
  background: rgba(16, 185, 129, 0.05);
}

.vx_data_table td {
  padding: 1.25rem 1.5rem;
  color: var(--text-primary);
  font-size: 0.9rem;
  font-weight: 600;
}

.vx_coin_info {
  display: flex;
  align-items: center;
  gap: 1rem;
}

.vx_coin_logo {
  width: 36px;
  height: 36px;
  border-radius: 50%;
  object-fit: cover;
  box-shadow: 0 4px 16px rgba(0, 0, 0, 0.4);
}

.vx_coin_name {
  font-weight: 700;
  letter-spacing: -0.01em;
}

.vx_stories_section {
  background: linear-gradient(135deg, var(--card-bg) 0%, var(--secondary-bg) 100%);
  border: 1px solid var(--border-color);
  border-radius: 20px;
  padding: 2rem;
  margin-bottom: 3rem;
  transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
  box-shadow: var(--shadow-lg);
  animation: fadeInUp 0.8s ease backwards;
  animation-delay: 1.2s;
}

.vx_stories_section:hover {
  border-color: var(--border-hover);
  transform: translateY(-4px);
  box-shadow: 0 16px 48px rgba(0, 0, 0, 0.6);
}

.vx_stories_header {
  margin-bottom: 2rem;
  padding-bottom: 1.5rem;
  border-bottom: 1px solid var(--border-color);
}

.vx_stories_header h3 {
  font-size: 1.25rem;
  font-weight: 800;
  color: var(--text-primary);
  display: flex;
  align-items: center;
  gap: 0.75rem;
  letter-spacing: -0.02em;
}

.vx_stories_header svg {
  width: 24px;
  height: 24px;
  color: var(--accent-emerald);
}

.vx_stories_list {
  display: flex;
  flex-direction: column;
  gap: 1.25rem;
}

.vx_story_item {
  padding: 1.5rem;
  background: rgba(255, 255, 255, 0.02);
  border: 1px solid var(--border-color);
  border-radius: 16px;
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  cursor: pointer;
  position: relative;
  overflow: hidden;
}

.vx_story_item::before {
  content: '';
  position: absolute;
  left: 0;
  top: 0;
  bottom: 0;
  width: 4px;
  background: linear-gradient(180deg, var(--accent-emerald), var(--accent-teal));
  transform: scaleY(0);
  transition: transform 0.3s ease;
}

.vx_story_item:hover::before {
  transform: scaleY(1);
}

.vx_story_item:hover {
  background: rgba(16, 185, 129, 0.05);
  border-color: var(--border-hover);
  transform: translateX(8px);
  box-shadow: 0 8px 24px rgba(0, 0, 0, 0.4);
}

.vx_story_time {
  display: inline-flex;
  align-items: center;
  gap: 0.5rem;
  font-size: 0.8rem;
  color: var(--text-muted);
  margin-bottom: 0.75rem;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.5px;
}

.vx_story_time svg {
  width: 16px;
  height: 16px;
}

.vx_story_title {
  font-size: 1rem;
  color: var(--text-primary);
  line-height: 1.6;
  font-weight: 600;
  letter-spacing: -0.01em;
  transition: color 0.3s ease;
}

.vx_story_item:hover .vx_story_title {
  color: var(--accent-emerald);
}

.modal {
  display: none;
  position: fixed;
  z-index: 1000;
  left: 0;
  top: 0;
  width: 100%;
  height: 100%;
  background: rgba(0, 0, 0, 0.9);
  backdrop-filter: blur(12px);
}

.modal.show {
  display: flex;
  align-items: center;
  justify-content: center;
}

.modal-content {
  background: linear-gradient(135deg, var(--card-bg) 0%, var(--secondary-bg) 100%);
  border: 1px solid var(--border-color);
  border-radius: 24px;
  padding: 2.5rem;
  max-width: 480px;
  width: 90%;
  box-shadow: 0 32px 80px rgba(0, 0, 0, 0.9);
  animation: modalSlideUp 0.4s cubic-bezier(0.4, 0, 0.2, 1);
}

@keyframes modalSlideUp {
  from {
    opacity: 0;
    transform: translateY(60px) scale(0.9);
  }
  to {
    opacity: 1;
    transform: translateY(0) scale(1);
  }
}

.modal-content h3 {
  font-size: 1.75rem;
  font-weight: 800;
  color: var(--text-primary);
  margin-bottom: 1.25rem;
  display: flex;
  align-items: center;
  gap: 0.75rem;
  letter-spacing: -0.02em;
}

.modal-content h3 svg {
  width: 32px;
  height: 32px;
  color: var(--danger-red);
}

.modal-content p {
  color: var(--text-secondary);
  margin-bottom: 2.5rem;
  line-height: 1.7;
  font-size: 1rem;
  font-weight: 500;
}

.modal-buttons {
  display: flex;
  gap: 1.25rem;
}

.btn {
  flex: 1;
  padding: 1.125rem 2rem;
  border: none;
  border-radius: 12px;
  font-weight: 700;
  font-size: 1.05rem;
  cursor: pointer;
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 0.625rem;
}

.btn svg {
  width: 20px;
  height: 20px;
}

.btn-danger {
  background: linear-gradient(135deg, var(--danger-red), #DC2626);
  color: white;
  box-shadow: 0 8px 24px rgba(239, 68, 68, 0.3);
}

.btn-danger:hover {
  transform: translateY(-2px);
  box-shadow: 0 12px 32px rgba(239, 68, 68, 0.4);
}

.btn-secondary {
  background: var(--card-elevated);
  border: 2px solid var(--border-color);
  color: var(--text-primary);
}

.btn-secondary:hover {
  background: var(--secondary-bg);
  border-color: var(--border-hover);
  transform: translateY(-2px);
}

.dashboard-content::-webkit-scrollbar {
  width: 10px;
}

.dashboard-content::-webkit-scrollbar-track {
  background: transparent;
}

.dashboard-content::-webkit-scrollbar-thumb {
  background: rgba(52, 119, 242, 0.2);
  border-radius: 5px;
}

.dashboard-content::-webkit-scrollbar-thumb:hover {
  background: rgba(52, 119, 242, 0.35);

@media (max-width: 768px) {
  .vx_header_title h1 {
    font-size: 1.75rem;
  }

  .vx_balance_amount {
    font-size: 2.75rem;
  }

  .vx_stats_row {
    gap: 1rem;
  }

  .vx_stat_card {
    padding: 1.5rem;
  }

  .vx_stat_value {
    font-size: 1.75rem;
  }

  .market-overview-header,
  .vx_table_header {
    padding: 1.25rem 1.5rem;
  }

  .vx_data_table th,
  .vx_data_table td {
    padding: 0.875rem 1rem;
    font-size: 0.8rem;
  }
}
  </style>
</head>
<body>
  <div class="dashboard-container">
    <%- include('../partials/user-nav.ejs', { user, page }) %>

    <main class="dashboard-content">
      <div class="vx_dashboard_header_section">
        <div class="vx_header_title">
          <h1>Welcome back, <%= user.fullName %>!</h1>
          <p>Your investment dashboard overview</p>
        </div>
        <a href="/user/real-estate-plans" class="vx_btn_invest_now">
          <i data-lucide="trending-up"></i>
          Invest Now
        </a>
      </div>

      <div class="vx_account_balance_card">
        <div class="vx_balance_icon">
          <i data-lucide="credit-card"></i>
        </div>
        <div class="vx_balance_title">Account Balance</div>
        <div class="vx_balance_subtitle">Your available funds</div>
        <div class="vx_balance_amount">$<%= Number(user.balance || 0).toLocaleString('en-US', { minimumFractionDigits: 2, maximumFractionDigits: 2 }) %></div>
        <div class="vx_balance_status success">
          <i data-lucide="check-circle"></i>
          Available for Withdrawal
        </div>
        <div class="vx_balance_status unverified">
          <i data-lucide="alert-triangle"></i>
          Unverified
        </div>
        <div class="vx_balance_updated">
          <i data-lucide="clock"></i>
          Last updated: <%= new Date().toLocaleString('en-US', { month: 'short', day: 'numeric', year: 'numeric', hour: 'numeric', minute: '2-digit', hour12: true }) %>
        </div>
        <div class="vx_balance_actions">
          <button class="vx_btn_deposit" onclick="window.location.href='/user/deposit'">
            <i data-lucide="arrow-down-circle"></i>
            Deposit
          </button>
          <button class="vx_btn_withdraw" onclick="window.location.href='/user/withdraw'">
            <i data-lucide="arrow-up-circle"></i>
            Withdraw
          </button>
        </div>
      </div>

      <div class="vx_active_engagements">
        <div class="vx_active_trade_card">
          <h4>Active Copy Trades</h4>
          <p><strong><%= (activeCopies || []).length %></strong> open copy strategies</p>
          <p><%= (activeCopies || []).length ? 'Your expert copy positions are shown below.' : 'No expert copy strategies are active right now.' %></p>
        </div>
        <div class="vx_active_trade_card">
          <h4>Active Bot Investments</h4>
          <p><strong><%= (activeInvestments || []).length %></strong> active bot investments</p>
          <p><%= (activeInvestments || []).length ? 'Your active bot trades are shown below.' : 'No automated bot investments are active at the moment.' %></p>
        </div>
      </div>

      <% const activeCopyList = activeCopies || []; const activeInvestmentList = activeInvestments || []; %>
      <div class="vx_compact_cards" style="display:grid;grid-template-columns:repeat(auto-fit,minmax(220px,1fr));gap:1rem;margin-bottom:1.25rem;">
        <% if (activeCopyList.length) { %>
          <% activeCopyList.forEach(function(copy) { %>
            <div class="vx_active_trade_card" style="padding:1rem;display:flex;flex-direction:column;gap:0.5rem;">
              <div style="display:flex;justify-content:space-between;align-items:center;">
                <div style="font-weight:700;color:#e2e8f0"><%= copy.expertName %></div>
                <div style="font-size:0.85rem;color:#94a3b8">Copy</div>
              </div>
              <% if (copy.expertTitle) { %>
              <div style="color:#94a3b8;font-size:0.9rem"><%= copy.expertTitle %></div>
              <% } %>
              <div style="display:flex;justify-content:space-between;align-items:center;">
                <div style="color:#94a3b8;font-size:0.9rem">Started: <%= new Date(copy.createdAt).toLocaleDateString('en-US') %></div>
                <div style="font-weight:700;color:#3b82f6">$<%= Number(copy.amount).toLocaleString('en-US', { minimumFractionDigits: 2, maximumFractionDigits: 2 }) %></div>
              </div>
              <div style="text-align:right;font-size:0.85rem;color:#22c55e;font-weight:700"><%= copy.status.charAt(0).toUpperCase() + copy.status.slice(1) %></div>
            </div>
          <% }); %>
        <% } %>

        <% if (activeInvestmentList.length) { %>
          <% activeInvestmentList.forEach(function(inv) { %>
            <div class="vx_active_trade_card" style="padding:1rem;display:flex;flex-direction:column;gap:0.5rem;">
              <div style="display:flex;justify-content:space-between;align-items:center;">
                <div style="font-weight:700;color:#e2e8f0"><%= inv.planName %></div>
                <div style="font-size:0.85rem;color:#94a3b8">Bot</div>
              </div>
              <div style="display:flex;justify-content:space-between;align-items:center;">
                <div style="color:#94a3b8;font-size:0.9rem">Started: <%= new Date(inv.createdAt).toLocaleDateString('en-US') %></div>
                <div style="font-weight:700;color:#3b82f6">$<%= Number(inv.amount).toLocaleString('en-US', { minimumFractionDigits: 2, maximumFractionDigits: 2 }) %></div>
              </div>
              <div style="text-align:right;font-size:0.85rem;color:#22c55e;font-weight:700"><%= inv.status.charAt(0).toUpperCase() + inv.status.slice(1) %></div>
            </div>
          <% }); %>
        <% } %>
      </div>

      <% if (activeCopyList.length || activeInvestmentList.length) { %>
        <div class="vx_active_trade_list">
          <% activeCopyList.forEach(function(copy) { %>
            <div class="vx_active_trade_list_row">
              <div class="vx_active_trade_list_item">
                <span class="vx_active_trade_tag">Copy Trade</span>
                <span><strong><%= copy.expertName %></strong></span>
                <span>Started on <%= new Date(copy.createdAt).toLocaleDateString('en-US') %></span>
              </div>
              <div class="vx_active_trade_list_item" style="text-align:right;">
                <span class="vx_active_trade_status"><%= copy.status.charAt(0).toUpperCase() + copy.status.slice(1) %></span>
                <span>$<%= Number(copy.amount).toLocaleString('en-US', { minimumFractionDigits: 2, maximumFractionDigits: 2 }) %></span>
              </div>
            </div>
          <% }); %>
          <% activeInvestmentList.forEach(function(inv) { %>
            <div class="vx_active_trade_list_row">
              <div class="vx_active_trade_list_item">
                <span class="vx_active_trade_tag">Bot Trade</span>
                <span><strong><%= inv.planName %></strong></span>
                <span>Started on <%= new Date(inv.createdAt).toLocaleDateString('en-US') %></span>
              </div>
              <div class="vx_active_trade_list_item" style="text-align:right;">
                <span class="vx_active_trade_status"><%= inv.status.charAt(0).toUpperCase() + inv.status.slice(1) %></span>
                <span>$<%= Number(inv.amount).toLocaleString('en-US', { minimumFractionDigits: 2, maximumFractionDigits: 2 }) %></span>
              </div>
            </div>
          <% }); %>
        </div>
      <% } %>

      <div class="vx_stats_row">
        <div class="vx_stat_card">
          <div class="vx_stat_header">
            <div class="vx_stat_icon">
              <i data-lucide="dollar-sign"></i>
            </div>
            <span class="vx_stat_label">Total Profit</span>
          </div>
          <div class="vx_stat_value">$<%= Number(user.totalProfit || 0).toLocaleString('en-US', { minimumFractionDigits: 2, maximumFractionDigits: 2 }) %></div>
          <div class="vx_stat_subtitle">
            <i data-lucide="calendar"></i>
            Last period
          </div>
        </div>
        <div class="vx_stat_card">
          <div class="vx_stat_header">
            <div class="vx_stat_icon">
              <i data-lucide="arrow-down"></i>
            </div>
            <span class="vx_stat_label">Total Deposit</span>
          </div>
          <div class="vx_stat_value">$<%= Number(user.totalDeposit || 0).toLocaleString('en-US', { minimumFractionDigits: 2, maximumFractionDigits: 2 }) %></div>
          <div class="vx_stat_subtitle">
            <i data-lucide="infinity"></i>
            All time
          </div>
        </div>
        <div class="vx_stat_card">
          <div class="vx_stat_header">
            <div class="vx_stat_icon">
              <i data-lucide="arrow-up"></i>
            </div>
            <span class="vx_stat_label">Total Withdrawal</span>
          </div>
          <div class="vx_stat_value">$<%= Number(user.totalWithdrawal || 0).toLocaleString('en-US', { minimumFractionDigits: 2, maximumFractionDigits: 2 }) %></div>
          <div class="vx_stat_subtitle">
            <i data-lucide="infinity"></i>
            All time
          </div>
        </div>
        <div class="vx_stat_card">
          <div class="vx_stat_header">
            <div class="vx_stat_icon">
              <i data-lucide="gift"></i>
            </div>
            <span class="vx_stat_label">Bonus</span>
          </div>
          <div class="vx_stat_value">$<%= Number(user.bonus || 0).toLocaleString('en-US', { minimumFractionDigits: 2, maximumFractionDigits: 2 }) %></div>
          <div class="vx_stat_subtitle">
            <i data-lucide="infinity"></i>
            All time
          </div>
        </div>
      </div>

      <div class="vx_verification_section">
        <div class="vx_verification_content">
          <div class="vx_verification_icon">
            <i data-lucide="shield-check"></i>
          </div>
          <div class="vx_verification_text">
            <h3>Identity Verification</h3>
            <p>Complete verification to access all features</p>
          </div>
        </div>
        <a href="/user/identity-verification" class="vx_btn_view_details">
          View Details
          <i data-lucide="arrow-right"></i>
        </a>
      </div>

      <div class="vx_layout_two_col">
        <div class="vx_col_left">
          <div class="vx_section_trades">
            <h3 class="vx_section_title">
              <i data-lucide="activity"></i>
              Latest Trades
            </h3>
            <table class="vx_trades_table">
              <thead>
                <tr>
                  <th>Details</th>
                  <th>Amount</th>
                  <th>Status</th>
                </tr>
              </thead>
              <tbody>
                <tr>
                  <td colspan="3" style="text-align: center; color: #9CA3AF; padding: 3rem; font-weight: 500;">No trades yet</td>
                </tr>
              </tbody>
            </table>
            <a href="#" class="vx_link_view_all">
              View All
              <i data-lucide="arrow-right"></i>
            </a>
          </div>
        </div>

        <div class="vx_col_right">
          <div class="vx_section_referrals">
            <h3 class="vx_section_title">
              <i data-lucide="users"></i>
              Referrals
            </h3>
            <p class="vx_referral_text">Present our project to your network and enjoy financial benefits. You don't need an active deposit to earn affiliate commissions.</p>
            <a href="#" class="vx_btn_learn_more">
              <i data-lucide="info"></i>
              Learn More
            </a>

            <div class="vx_referral_link_box">
              <p class="vx_referral_label">
                <i data-lucide="link"></i>
                Personal Referral Link
              </p>
              <div class="vx_referral_link_input">
                <input type="text" value="https://velocityx-ai.com/ref/babatunde@20." readonly>
                <button class="vx_btn_copy">
                  <i data-lucide="copy"></i>
                  Copy
                </button>
              </div>
            </div>
          </div>
        </div>
      </div>

      <div class="vx_market_tickers_section">
        <div class="vx_market_tickers" id="marketTickersContainer">
        </div>
      </div>

      <div class="market-overview-section">
        <div class="market-overview-header">
          <h3 class="market-overview-title">
            <i data-lucide="bar-chart-3"></i>
            Market Overview
          </h3>
          <a href="#" class="market-overview-link">
            Open Markets
            <i data-lucide="external-link"></i>
          </a>
        </div>
        <div style="padding:0;">
          <div class="trading-chart-container">
            <div class="chart-widget" id="tradingviewWidgetDash"></div>
          </div>
        </div>
      </div>

      <div class="vx_table_section">
        <div class="vx_table_header">
          <i data-lucide="bitcoin"></i>
          <h3>CRYPTO</h3>
        </div>
        <table class="vx_data_table">
          <thead>
            <tr>
              <th>Name</th>
              <th>Value</th>
              <th>Change</th>
              <th>Chg%</th>
              <th>Open</th>
              <th>High</th>
              <th>Low</th>
              <th>Prev</th>
            </tr>
          </thead>
          <tbody>
            <tr>
              <td>
                <div class="vx_coin_info">
                  <img src="https://cryptologos.cc/logos/bitcoin-btc-logo.png" alt="BTC" class="vx_coin_logo">
                  <span class="vx_coin_name">Bitcoin</span>
                </div>
              </td>
              <td>84,496.27</td>
              <td style="color: #ef4444;">-4,803.73</td>
              <td style="color: #ef4444;">-5.38%</td>
              <td>89,249.99</td>
              <td>90,488.00</td>
              <td>84,196.00</td>
              <td>89,300.00</td>
            </tr>
            <tr>
              <td>
                <div class="vx_coin_info">
                  <img src="https://cryptologos.cc/logos/ethereum-eth-logo.png" alt="ETH" class="vx_coin_logo">
                  <span class="vx_coin_name">Ethereum</span>
                </div>
              </td>
              <td>2,816.23</td>
              <td style="color: #ef4444;">-194.59</td>
              <td style="color: #ef4444;">-6.47%</td>
              <td>3,026.77</td>
              <td>3,045.78</td>
              <td>2,790.00</td>
              <td>3,010.82</td>
            </tr>
            <tr>
              <td>
                <div class="vx_coin_info">
                  <img src="https://cryptologos.cc/logos/bnb-bnb-logo.png" alt="BNB" class="vx_coin_logo">
                  <span class="vx_coin_name">BNB</span>
                </div>
              </td>
              <td>866.10</td>
              <td style="color: #ef4444;">-36.62</td>
              <td style="color: #ef4444;">-4.06%</td>
              <td>898.35</td>
              <td>909.43</td>
              <td>860.15</td>
              <td>902.72</td>
            </tr>
          </tbody>
        </table>
      </div>

      <div class="vx_table_section">
        <div class="vx_table_header">
          <i data-lucide="trending-up"></i>
          <h3>FOREX</h3>
        </div>
        <table class="vx_data_table">
          <thead>
            <tr>
              <th>Name</th>
              <th>Value</th>
              <th>Change</th>
              <th>Chg%</th>
              <th>Open</th>
              <th>High</th>
              <th>Low</th>
              <th>Prev</th>
            </tr>
          </thead>
          <tbody>
            <tr>
              <td>
                <div class="vx_coin_info">
                  <img src="https://upload.wikimedia.org/wikipedia/commons/b/b7/Flag_of_Europe.svg" alt="EUR" class="vx_coin_logo">
                  <span class="vx_coin_name">EUR/USD</span>
                </div>
              </td>
              <td>1.19703</td>
              <td style="color: #ef4444;">-0.01</td>
              <td style="color: #ef4444;">-0.92%</td>
              <td>1.20391</td>
              <td>1.20456</td>
              <td>1.19188</td>
              <td>1.20391</td>
            </tr>
            <tr>
              <td>
                <div class="vx_coin_info">
                  <img src="https://upload.wikimedia.org/wikipedia/commons/a/a5/Flag_of_the_United_Kingdom_%281-2%29.svg" alt="GBP" class="vx_coin_logo">
                  <span class="vx_coin_name">GBP/USD</span>
                </div>
              </td>
              <td>1.38081</td>
              <td style="color: #ef4444;">-0.01</td>
              <td style="color: #ef4444;">-0.50%</td>
              <td>1.38422</td>
              <td>1.38461</td>
              <td>1.37492</td>
              <td>1.38422</td>
            </tr>
            <tr>
              <td>
                <div class="vx_coin_info">
                  <img src="https://upload.wikimedia.org/wikipedia/en/9/9e/Flag_of_Japan.svg" alt="JPY" class="vx_coin_logo">
                  <span class="vx_coin_name">USD/JPY</span>
                </div>
              </td>
              <td>153.642</td>
              <td style="color: #3477F2;">1.48</td>
              <td style="color: #3477F2;">0.97%</td>
              <td>152.165</td>
              <td>153.784</td>
              <td>152.141</td>
              <td>152.165</td>
            </tr>
          </tbody>
        </table>
      </div>

      <div class="vx_stories_section">
        <div class="vx_stories_header">
          <h3>
            <i data-lucide="newspaper"></i>
            Top Stories
          </h3>
        </div>
        <div class="vx_stories_list">
          <div class="vx_story_item">
            <span class="vx_story_time">
              <i data-lucide="clock" style="color: #f59e0b;"></i>
              7 hours ago
            </span>
            <p class="vx_story_title">XAU/USD: Gold Goes Vertical as Prices Slice Through $5,300 on Global Rush to Safety</p>
          </div>
          <div class="vx_story_item">
            <span class="vx_story_time">
              <i data-lucide="clock" style="color: #3477F2;"></i>
              8 hours ago
            </span>
            <p class="vx_story_title">DXY: Dollar Hits Four-Year Low After Trump Says US Currency is "Doing Great"</p>
          </div>
          <div class="vx_story_item">
            <span class="vx_story_time">
              <i data-lucide="clock" style="color: #ef4444;"></i>
              8 hours ago
            </span>
            <p class="vx_story_title">SPX: S&P 500 Breaks Into Record High as Magnificent Seven Rallies (with One Exception)</p>
          </div>
          <div class="vx_story_item">
            <span class="vx_story_time">
              <i data-lucide="clock" style="color: #9CA3AF;"></i>
              yesterday
            </span>
            <p class="vx_story_title">GME: GameStop Adds to Strong Start of Year After Michael Burry Reveals Stake</p>
          </div>
        </div>
      </div>
    </main>
  </div>

  <div id="logoutModal" class="modal">
    <div class="modal-content">
      <h3>
        <i data-lucide="alert-circle"></i>
        Confirm Logout
      </h3>
      <p>Are you sure you want to log out of your account?</p>
      <div class="modal-buttons">
        <button id="confirmLogout" class="btn btn-danger">
          <i data-lucide="log-out"></i>
          Yes, Logout
        </button>
        <button id="cancelLogout" class="btn btn-secondary">
          <i data-lucide="x"></i>
          Cancel
        </button>
      </div>
    </div>
  </div>

  <script>
function initTradingViewWidgetDash() {
  const widgetContainer = document.getElementById('tradingviewWidgetDash');
  if (widgetContainer && typeof TradingView !== 'undefined') {
    try {
      new TradingView.widget({
        autosize: true,
        symbol: 'BITSTAMP:BTCUSD',
        interval: 'D',
        timezone: 'Etc/UTC',
        theme: 'dark',
        style: '1',
        locale: 'en',
        toolbar_bg: '#0B0F19',
        enable_publishing: false,
        allow_symbol_change: true,
        container_id: 'tradingviewWidgetDash'
      });
    } catch (e) {
      console.error('TradingView widget error:', e);
    }
  }
}

(function () {
  const s = document.createElement('script');
  s.src = 'https://s3.tradingview.com/tv.js';
  s.async = true;
  s.onload = initTradingViewWidgetDash;
  document.head.appendChild(s);
  setTimeout(function () {
    if (!document.querySelector('#tradingviewWidgetDash iframe')) initTradingViewWidgetDash();
  }, 2100);
})();

const marketPairs = [
  {
    symbol: 'BTCUSDT',
    name: 'BTC/USDT',
    logo: 'https://cryptologos.cc/logos/bitcoin-btc-logo.png',
    price: 69398.91,
    change: -5.15,
    exchange: 'Binance',
    tvLink: 'https://www.tradingview.com/symbols/BTCUSDT/?utm_campaign=mini-symbol-overview&utm_medium=widget&utm_source=velocityx-ai.com'
  },
  {
    symbol: 'ETHUSDT',
    name: 'ETH/USDT',
    logo: 'https://cryptologos.cc/logos/ethereum-eth-logo.png',
    price: 2054.20,
    change: -4.38,
    exchange: 'Binance',
    tvLink: 'https://www.tradingview.com/symbols/ETHUSDT/?utm_campaign=mini-symbol-overview&utm_medium=widget&utm_source=velocityx-ai.com'
  },
  {
    symbol: 'EURUSD',
    name: 'EUR/USD',
    logo: 'https://upload.wikimedia.org/wikipedia/commons/b/b7/Flag_of_Europe.svg',
    price: 1.18048,
    change: -0.02,
    exchange: 'Forex',
    tvLink: 'https://www.tradingview.com/symbols/EURUSD/?utm_campaign=mini-symbol-overview&utm_medium=widget&utm_source=velocityx-ai.com'
  },
  {
    symbol: 'GBPUSD',
    name: 'GBP/USD',
    logo: 'https://upload.wikimedia.org/wikipedia/commons/a/a5/Flag_of_the_United_Kingdom_%281-2%29.svg',
    price: 1.35762,
    change: -0.50,
    exchange: 'Forex',
    tvLink: 'https://www.tradingview.com/symbols/GBPUSD/?utm_campaign=mini-symbol-overview&utm_medium=widget&utm_source=velocityx-ai.com'
  },
  {
    symbol: 'AAPL',
    name: 'AAPL',
    logo: 'https://logo.clearbit.com/apple.com',
    price: 276.49,
    change: 1.24,
    exchange: 'NASDAQ',
    tvLink: 'https://www.tradingview.com/symbols/AAPL/?utm_campaign=mini-symbol-overview&utm_medium=widget&utm_source=velocityx-ai.com'
  },
  {
    symbol: 'XAUUSD',
    name: 'GOLD',
    logo: 'https://img.icons8.com/color/96/000000/gold-bars.png',
    price: 4832.830,
    change: 2.85,
    exchange: 'Commodities',
    tvLink: 'https://www.tradingview.com/symbols/XAUUSD/?utm_campaign=mini-symbol-overview&utm_medium=widget&utm_source=velocityx-ai.com'
  }
];

function createSparklineCanvas(change) {
  const canvas = document.createElement('canvas');
  canvas.width = 120;
  canvas.height = 48;
  const ctx = canvas.getContext('2d');

  const points = 25;
  const data = [];
  let value = 50;

  for (let i = 0; i < points; i++) {
    value += (Math.random() - 0.5) * 10;
    value = Math.max(10, Math.min(90, value));
    data.push(value);
  }

  if (change > 0) {
    data[data.length - 1] = Math.max(...data);
  } else {
    data[data.length - 1] = Math.min(...data);
  }

  ctx.strokeStyle = change > 0 ? '#3477F2' : '#ef4444';
  ctx.lineWidth = 2.5;
  ctx.lineCap = 'round';
  ctx.lineJoin = 'round';

  ctx.beginPath();
  for (let i = 0; i < data.length; i++) {
    const x = (i / (data.length - 1)) * canvas.width;
    const y = canvas.height - (data[i] / 100) * canvas.height;

    if (i === 0) {
      ctx.moveTo(x, y);
    } else {
      ctx.lineTo(x, y);
    }
  }
  ctx.stroke();

  const gradient = ctx.createLinearGradient(0, 0, 0, canvas.height);
  gradient.addColorStop(0, change > 0 ? 'rgba(16, 185, 129, 0.25)' : 'rgba(239, 68, 68, 0.25)');
  gradient.addColorStop(1, 'rgba(16, 185, 129, 0)');

  ctx.lineTo(canvas.width, canvas.height);
  ctx.lineTo(0, canvas.height);
  ctx.closePath();
  ctx.fillStyle = gradient;
  ctx.fill();

  return canvas;
}

function renderMarketTickers() {
  const container = document.getElementById('marketTickersContainer');
  if (!container) return;

  container.innerHTML = '';

  marketPairs.forEach(pair => {
    const changeClass = pair.change > 0 ? 'up' : 'down';
    const changeSign = pair.change > 0 ? '+' : '';
    const decimals = pair.price > 1000 ? 2 : pair.price > 10 ? 3 : 5;

    const card = document.createElement('a');
    card.href = pair.tvLink;
    card.target = '_blank';
    card.rel = 'noopener noreferrer';
    card.className = 'vx_ticker_card';

    card.innerHTML = `
      <div class="vx_ticker_header">
        <img src="${pair.logo}" alt="${pair.name}" class="vx_ticker_logo" onerror="this.style.display='none'">
        <div class="vx_ticker_info">
          <div class="vx_ticker_pair">${pair.name}</div>
          <div class="vx_ticker_exchange">${pair.exchange}</div>
        </div>
      </div>
      <div class="vx_ticker_price">$${pair.price.toLocaleString('en-US', { minimumFractionDigits: decimals, maximumFractionDigits: decimals })}</div>
      <div class="vx_ticker_change ${changeClass}">
        <i data-lucide="${pair.change > 0 ? 'trending-up' : 'trending-down'}"></i>
        ${changeSign}${pair.change.toFixed(2)}%
      </div>
      <div class="vx_ticker_chart"></div>
    `;

    const chartContainer = card.querySelector('.vx_ticker_chart');
    const sparkline = createSparklineCanvas(pair.change);
    chartContainer.appendChild(sparkline);

    container.appendChild(card);
  });

  lucide.createIcons();
}

function updateMarketTickers() {
  marketPairs.forEach(pair => {
    const randomChange = (Math.random() - 0.5) * 0.3;
    pair.change = Math.max(-10, Math.min(10, pair.change + randomChange));
    pair.price = pair.price * (1 + randomChange / 100);
  });
  renderMarketTickers();
}

function initializeCopyButton() {
  const copyBtn = document.querySelector('.vx_btn_copy');
  if (!copyBtn) return;

  copyBtn.addEventListener('click', function () {
    const input = document.querySelector('.vx_referral_link_input input');
    if (input) {
      input.select();
      document.execCommand('copy');

      const icon = this.querySelector('i');
      const originalIcon = icon.getAttribute('data-lucide');

      icon.setAttribute('data-lucide', 'check');
      lucide.createIcons();

      const originalText = this.childNodes[1].textContent;
      this.childNodes[1].textContent = 'Copied!';

      setTimeout(() => {
        icon.setAttribute('data-lucide', originalIcon);
        lucide.createIcons();
        this.childNodes[1].textContent = originalText;
      }, 2000);
    }
  });
}

function initializeModal() {
  const modal = document.getElementById('logoutModal');
  if (!modal) return;

  const confirmBtn = document.getElementById('confirmLogout');
  const cancelBtn = document.getElementById('cancelLogout');

  const logoutButtons = document.querySelectorAll('[data-action="logout"]');
  logoutButtons.forEach(btn => {
    btn.addEventListener('click', e => {
      e.preventDefault();
      modal.classList.add('show');
    });
  });

  if (cancelBtn) {
    cancelBtn.addEventListener('click', () => {
      modal.classList.remove('show');
    });
  }

  if (confirmBtn) {
    confirmBtn.addEventListener('click', () => {
      window.location.href = '/logout';
    });
  }

  modal.addEventListener('click', e => {
    if (e.target === modal) {
      modal.classList.remove('show');
    }
  });
}

document.addEventListener('DOMContentLoaded', function () {
  lucide.createIcons();

  renderMarketTickers();
  updateMarketTickers();
  setInterval(updateMarketTickers, 8000);

  initializeCopyButton();
  initializeModal();
});
  </script>

  <script src="/js/dashboard.js"></script>
</body>
</html>
